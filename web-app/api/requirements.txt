# MPAC Web App — FastAPI backend
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sqlalchemy>=2.0
bcrypt>=4.0
pyjwt>=2.8
anthropic>=0.40.0
websockets>=14.0
python-multipart>=0.0.9
# Per-user BYOK Anthropic key encryption (Fernet)
cryptography>=42.0
# MPAC protocol core + MCP bridge (both on PyPI as of 2026-04-17)
# mpac 0.2.4 extends attribute-chain resolution to `from pkg import mod`
# + `mod.attr()` — the most common Python idiom for submodule access.
# Without this, internal-beta scenario 4 would never flag the importer.
# mpac 0.2.5 adds INTENT_DEFERRED + Participant.defer_intent — needed by
# the v0.2.9 mpac-mcp defer_intent MCP tool and the new
# /api/agent/intents/defer endpoint.
# mpac 0.2.6 makes the deferral cleanup-on-terminate path match by
# observed_principals OR principal-id-mistakenly-in-observed-intent-ids
# fields, so 0.2.5 relays that misfilled the field still get clean
# auto-resolve when the observed intent withdraws (verified necessary
# 2026-04-29 e2e test).
mpac>=0.2.6
# Pinned to match `_MIN_MPAC_MCP` in api/routes/agent.py — bootstrap
# auto-upgrades clients to this floor. Server install must satisfy the
# same minimum or stale dev wheels can bypass the 0.2.4 relay error
# decoding (stderr+stdout merge). Bump both together.
# 0.2.5: relay /ws/relay auth via Authorization header instead of
# ``?token=`` query — closes the URL-leak surface the v3 review flagged.
# 0.2.6: relay calls /api/agent/intents/withdraw_all on subprocess
# timeout / exit-non-zero so orphan intents (typical case: API content
# filter blocks the followup write) get cleaned up before the next
# retry. Backward compatible: 0.2.5 relays still connect.
# 0.2.7: relay exposes list_my_active_intents() + withdraw_all_my_intents()
# MCP tools so a fresh `claude -p` subprocess can rediscover and clean
# up intents from earlier turns.
# 0.2.8: relay preserves full conversation memory across turns via
# `claude --resume <id>` + JSON output capture.
# 0.2.9: relay exposes defer_intent MCP tool (backed by new
# /api/agent/intents/defer endpoint) so a Claude that yields after
# check_overlap can surface a "yield" chip in siblings' Conflicts panel.
# See _MIN_MPAC_MCP comment in routes/agent.py for the full story.
mpac-mcp>=0.2.9
