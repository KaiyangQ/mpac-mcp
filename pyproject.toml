[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "mpac-reference-runtime"
version = "0.1.0"
description = "Reference runtime for the Multi-Principal Agent Coordination Protocol"
readme = "README.md"
requires-python = ">=3.9"
