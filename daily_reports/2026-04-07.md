# Daily Report — 2026-04-07

## 概述

今天在 MPAC 协议中新增了 **Backend Health Monitoring** 功能（v0.1.13），集成 aistatus.cc 的开源 AI 状态监控 API，使 agent 能够声明后端 AI 模型依赖、汇报后端健康状态，并通过 coordinator 进行告警和模型切换治理。

---

## 完成事项

### 1. Backend Health Monitoring 功能设计与实现

**新增协议扩展（全部向后兼容）：**

- **HELLO 扩展**：新增可选 `backend` 字段（`model_id`、`provider`），agent 加入 session 时声明 AI 模型依赖
- **HEARTBEAT 扩展**：新增可选 `backend_health` 字段（`model_id`、`provider_status`、`status_detail`、`checked_at`、`alternatives`、`switched_from`、`switch_reason`），定期汇报后端状态
- **COORDINATOR_STATUS 扩展**：`event` 枚举新增 `backend_alert`，附带 `affected_principal` 和 `backend_detail` 条件字段
- **Liveness Policy 扩展**：`backend_health_policy` 对象（`enabled`、`check_source`、`check_interval_sec`、`on_degraded`、`on_down`、`auto_switch`、`allowed_providers`）
- **新增错误码**：`BACKEND_SWITCH_DENIED`（auto_switch=forbidden 或 provider 不在白名单）
- **新增 SPEC Section 14.3.1**：完整的行为规范，包括 coordinator 动作（ignore/warn/suspend_and_claim）和模型切换治理（allowed/notify_first/forbidden）

**协议 vs 实现的职责边界：**
- 协议管：信号机制（声明、汇报、告警）+ 治理规则（白名单、切换策略）
- 实现管：选哪个替代模型、什么时候触发切换、是否切回

**`provider_status` 枚举和 `alternatives` 结构直接对齐 aistatus.cc `/api/check` 响应格式。**

### 2. JSON Schema 更新

更新 6 个 schema 文件：
- `hello.schema.json`：加 `backend` object
- `heartbeat.schema.json`：加 `backend_health` object，含 `provider_status` 枚举（operational/degraded/down/unknown）
- `coordinator_status.schema.json`：`event` 枚举加 `backend_alert`，新增 if/then 条件约束
- `session_info.schema.json`：`liveness_policy` 加 `backend_health_policy` 完整定义
- `protocol_error.schema.json`：error_code 枚举加 `BACKEND_SWITCH_DENIED`
- `envelope.schema.json`：版本号更新

### 3. 参考实现更新

**Python（`ref-impl/python/`）：**
- `models.py`：新增 `BACKEND_ALERT`（CoordinatorEvent）、`BACKEND_SWITCH_DENIED`（ErrorCode）、`BackendProviderStatus`、`BackendSwitchReason`、`BackendHealthAction`、`AutoSwitchPolicy` 枚举
- `coordinator.py`：`ParticipantInfo` 新增 backend 字段、`_handle_hello` 存储 backend 信息、`_handle_heartbeat` 处理 backend_health、新增 `_process_backend_health`/`_validate_backend_switch`/`_build_liveness_policy`/`_make_coordinator_status` 方法
- 全部 109 测试通过

**TypeScript（`ref-impl/typescript/`）：**
- `models.ts`：新增对应枚举
- `coordinator.ts`：`ParticipantInfo` 新增 backend 字段、`handleHeartbeat` 处理 backend_health、新增 `processBackendHealth`/`validateBackendSwitch`/`buildLivenessPolicy`/`makeCoordinatorStatusBackendAlert` 方法
- 全部 88 测试通过

### 4. 文档同步

- Version History Update Record（`v0.1.13_backend_health_monitoring/`）
- SPEC 快照（`SPEC_v0.1.13_2026-04-07.md`）
- CHANGELOG.md（新增 v0.1.13 条目）
- MPAC_Developer_Reference.md（payload 表、枚举、错误码）
- README.md（版本号、Coverage 表）
- blog/introducing-mpac.md（版本号、功能列表）

---

## 当前项目文件结构

无结构变化，仅在 `version_history/` 下新增 `v0.1.13_backend_health_monitoring/` 文件夹。

---

## 后续计划

- 新增 backend health demo transcript（后端宕机 → 告警 → intent 转移完整流程）
- 考虑为 backend health monitoring 编写专项测试
- 探索 aistatus.cc MCP 集成方式
