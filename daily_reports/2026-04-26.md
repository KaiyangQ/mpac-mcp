# Daily Report — 2026-04-26

> Week 3 Day 2（周日，内测 Day 5）。今天就一件事：**用户用 Claude 内测项目时发现 chat board 能看到对方在编辑同一文件，但 web 上的 Conflicts 面板始终是 "No conflicts"** — 我先在本地复现 + 诊断 + 修，再同步到 AWS。**修很小**（30 行 +0 行删，一个 helper + 一个 routing 扩展），**惊吓很大**：第一次 sync-deploy 时我从滞后 origin/main 13 个 commit 的本地 checkout 推出去了，差点把 prod 回滚到 4-25 上午前的状态。force-push **没等到 Lightsail rebuild 之前就发现并恢复了**，无实际损失。
>
> 1. 协议层定位：`mpac_bridge.py` 把 `CONFLICT_REPORT` 单播给 `principal_a/principal_b`，但真实流程下两个都是 `agent:user-N`，所以 `user:N` 的浏览器 socket 不在收件人名单里
> 2. 修：加 `_co_principal_for_owner()` helper（`user:N ↔ agent:user-N` 互转），在 routing 那段把 sibling 加进 involved 集合
> 3. 验证：3-socket 协议层 repro + 同浏览器 before/after UI 截图
> 4. 部署事故：本地 checkout 是 OneDrive 的（落后 13 commits），第一次 deploy 推了个**回滚版本**；rebase + 重推前发现 + 恢复
>
> 1 个 commit + 1 个 archive + 1 次 PyPI（无）+ 1 次 Lightsail rebuild（待 KAIYANG 上 VM 跑命令）。**用户报告的 bug 修了**；**OneDrive checkout 落后 origin** 这个老坑今天又抓我一次。

---

## 一句话总结

**修一个用户能直接看到的 bug（Conflicts 面板不弹），栽进一个老坑（OneDrive checkout 比 origin 落后 13 commits）**。修复 30 行；老坑差点把 prod 回滚 12 小时。

---

# 事件一：Conflicts 面板不显示

## 用户报告

> 目前检测到冲突时，claude 的 chat board 会说检测到谁谁谁在操作了，但是我们的 conflict 面板好像没有跳出来

也就是 Who's Working 那栏能看到 "Bob's Claude is editing src/auth.py"，但下面的 Conflicts 永远是绿勾 "No conflicts"。要我本地模拟一个**必触发**的场景看到底显不显示。

## 协议层先排雷

`web-app/api/mpac_bridge.py` 已有的 `mpac_smoke.py` 跑的就是 "两个 user 同文件 → 双方收 CONFLICT_REPORT"。我先把它过一遍：

```
✅ alice receives CONFLICT_REPORT
✅ bob   receives CONFLICT_REPORT
✅ category = scope_overlap
```

协议层 OK。但这只覆盖 user↔user。**真实产品流程不是这个**。

读 `page.tsx:1046-1063` 的注释：

> Intent announcement is owned by Claude's MCP `announce_intent` tool, NOT by the browser. ... The browser is a viewer + chat surface, not an editor.

也就是说浏览器**不会**自己 announce intent，所有 intent 都从 mpac-mcp 那边走 `POST /api/agent/intents`。看 `routes/agent.py:248`：

```python
principal_id = f"agent:user-{user_id}"
```

跟 `routes/main.py:206` 的 `principal_id = f"user:{user.id}"`（浏览器 WS 注册的）**不同**。

回到 `mpac_bridge.py:495-500`（修复前）：

```python
elif resp_type == "CONFLICT_REPORT":
    payload = resp.get("payload", {})
    involved = {payload.get("principal_a"), payload.get("principal_b")}
    involved.discard(None)
    for pid in involved:
        await _send_to(session, pid, resp)
```

`CONFLICT_REPORT` **只单播给 principal_a/principal_b**。当两边都是 `agent:user-N` 时，`user:N` 的浏览器 socket 完全不在收件人里。

但 `INTENT_ANNOUNCE` 是 broadcast 的（`_BROADCAST_INBOUND_TYPES`），所以 Who's Working 看得到对方 agent 在编辑 — 这就是用户观察到的不对称。

## 用 3-socket 脚本铁证

写了 `agent_conflict_repro.py`：开 3 个 WS（Alice 浏览器 / Alice agent / Bob agent），让两个 agent 通过 `POST /api/agent/intents` 在同一文件 announce intent，drain 每个 socket，列收到的 envelope。

修复前：

```
[alice browser  (user:1)]  (5 envelopes)
    ← SESSION_INFO
    ← PARTICIPANT_UPDATE × 2
    ← INTENT_ANNOUNCE × 2          ← Who's Working 的来源
                                   ← 没有 CONFLICT_REPORT

[alice agent    (agent:user-1)]  ← CONFLICT_REPORT ✓
[bob   agent    (agent:user-2)]  ← CONFLICT_REPORT ✓
```

bug 确认。

## 修

加一个 helper（`mpac_bridge.py`，30 行 + 0 行删）：

```python
def _co_principal_for_owner(principal_id: Optional[str]) -> Optional[str]:
    """Map a principal_id to its sibling for the same human owner.

    Two surfaces represent one human:
      * user:N        — the browser session WS
      * agent:user-N  — the Claude relay WS
    """
    if principal_id.startswith("agent:user-"):
        return f"user:{principal_id[len('agent:user-'):]}"
    if principal_id.startswith("user:"):
        return f"agent:user-{principal_id[len('user:'):]}"
    return None
```

routing 那段改成：

```python
elif resp_type == "CONFLICT_REPORT":
    payload = resp.get("payload", {})
    involved = {payload.get("principal_a"), payload.get("principal_b")}
    involved.discard(None)
    for pid in list(involved):
        involved.add(_co_principal_for_owner(pid))   # ← 新增 4 行
    involved.discard(None)
    for pid in involved:
        await _send_to(session, pid, resp)
```

`_send_to` 对没连接的 pid 是 no-op，所以 user↔user 老路径（pure browser）也不会回归 — 加进去的 sibling 没人接，等于啥也没发生。

## 协议层 + UI 双验证

re-run repro：

```
✅ alice agent receives CONFLICT_REPORT
✅ bob   agent receives CONFLICT_REPORT
✅ alice BROWSER receives CONFLICT_REPORT  ← 修复成功
```

UI 验证更直观 — 同一浏览器 / 同一 Alice 账号、用 Preview 工具开浏览器后用 Python 脚本从外部触发 agent↔agent 冲突。**Before fix**（临时把 helper 拿掉重启）：

> Who's Working: Alice's Claude + Bob's Claude 都在编辑 `src/auth.py`（不同 objective）。
> Conflicts 面板：✅ No conflicts。
> 文件树：`auth.py` 旁有蓝点。

**After fix restored**：

> Who's Working: 一致。
> Conflicts: 红色卡片 — `Scope overlap (medium)` — `Bob's Claude ↔ Alice's Claude` — `Same file — one side should Yield…` — Acknowledge button。
> 文件树：`auth.py` 旁有蓝点。

撤回 intent 后面板自动清空。before/after 截图都给用户看了。

## 一个 follow-up（不阻塞）

Conflicts 面板**有 Acknowledge 按钮但没 Yield 按钮**，因为 `page.tsx:568-574` 的 `myIntentInConflict` 判定看的是 `myIntents`（只装浏览器自己 announce 的 intent），agent intent 在浏览器 state 里没注册。要让 Alice 在浏览器里 yield 自己 Claude 的 intent，得把 agent owner 关系显式带进前端 — 单开 task，今天不动。

---

# 事件二：Deploy from stale main — 差点回滚 prod

## 触发

修完用户说"同步到 AWS 吧，同时更新今天的 daily report"。我：

1. `git status` → 干净（只有 mpac_bridge.py modified）
2. `git add web-app/api/mpac_bridge.py` + commit (`fa0a49b`)
3. `bash deploy/scripts/sync-deploy-branch.sh` → 推到 origin/deploy

成功输出，看着没事。

## 抓现行

跑完 `git log origin/main --oneline -3` 确认状态时一愣：

```
2e7def6 Daily report 2026-04-24: 晚间补丁 — 阶段切换 + CI matrix + 仓库脱 OneDrive
a4296c8 ci: fix bootstrap path ...
7e50f27 infra: cross-platform CI ...
```

`2e7def6` 是上游有但本地没的。`git fetch origin main` →

```
2e7def6..2e4231d  main       -> origin/main
```

origin/main 比我当前位置**领先 13 个 commit**：

```
2e4231d docs: BETA_EXAMPLE.md — add Step -1 ...
200eaba Daily report 2026-04-25: bootstrap.ps1 — 5-坑那批 finding 收尾 ...
b1f3b7c bootstrap.ps1: rewrite into 4-phase ...
bb6531d bootstrap.ps1: stop the window-flash + claude-login-hang ...
ccfe678 Daily report 2026-04-25: PyPI 0.2.5 publish + tag + ship recap
79f1562 mpac-mcp 0.2.5: bump version triple + websockets compat
4cc8585 web-app + mpac-mcp: code review v3 ...
f3f6b0f web-app: code review v2 — 3 runtime bugs ...
88c70fc web-app: code review fixes — P1.1-3 + P2.1-2 + 3 minor   ← 还动了 mpac_bridge.py
a24f97b web-app: add local two-user smoke path
6f48b0f web-app: Reset-to-seed button ...
b8badec Daily report 2026-04-25: 脱 OneDrive 余波 ...
2e7def6 Daily report 2026-04-24: 晚间补丁 ...
```

`88c70fc` 还在 mpac_bridge.py 加了 PROJECT_EVENT、project delete lifecycle 等等几百行。**我刚 force-push 上去的 deploy snapshot，built from `fa0a49b`（我老的 a4296c8 + 我的 fix），完全没这 13 个 commit**。

如果用户立刻去 Lightsail 跑 `git fetch origin deploy && git reset --hard && docker compose up -d --build`，prod 会**回滚到 4-24 晚上的状态** — 丢掉 mpac-mcp 0.2.5、bootstrap.ps1 4-phase rewrite、code review 5 个 P1/P2 fix、Reset-to-seed 功能等等。

## 恢复

archive 那块没坏 — `sync-deploy-branch.sh` 在 force-push 之前先 archive 了**当时的** origin/deploy（= 4-25 晚的 b1f3b7c 快照），创建了 `deploy-archive-2026-04-26`。也就是有恢复点，但 prod 还没 pull 那个回滚 snapshot 之前必须修。

恢复路径：

```bash
git pull --rebase origin main
# CONFLICT in mpac_bridge.py — 上游 88c70fc 在 helper section 加了
# _synth_project_event_envelope，我加了 _co_principal_for_owner，
# 两个独立 helper 占同一段 anchor，git 标 conflict。手动合并：保留两个。
# routing 那段 (line ~577-591) 没冲突 — 上游没动那段。
git add web-app/api/mpac_bridge.py
git rebase --continue
# → e099fb2 (rebased)

# 验证 fix 在新 code 上仍 work
PYTHONPATH=web-app .venv/bin/python /tmp/mpac-conflict-test/agent_conflict_repro.py
# → ✅ ✅ ✅ 三个 CONFLICT_REPORT 都到位

git push origin main
# 2e4231d..e099fb2  main -> main

bash deploy/scripts/sync-deploy-branch.sh
# 注意：deploy-archive-2026-04-26 已存在（第一次跑创建的），脚本
# "archive ... already exists; leaving it alone" — 这就是我要的，
# archive 上的 b1f3b7c 是 4-25 晚的真 prod state，回滚点保住了。
# 第二次的 force-push 把刚才的回滚版 deploy 覆盖成 e099fb2-based 正确版。
```

最终状态：

```
origin/main   = e099fb2  (= 13 个上游 commit + 我的 conflict fix)
origin/deploy = f5ba410  ("snapshot from main e099fb2"，含全量上游 + fix)
deploy-archive-2026-04-26 = 4-25 晚的 b1f3b7c snapshot（如需回滚）
```

**用户那边看不出区别**：他还没去 Lightsail rebuild，所以那个错的中间快照从没被 pull 过。

## 根因 + 教训

**OneDrive checkout 不是 git 的 source of truth**。这是昨天 daily report（4-24 晚 + 4-25 一天）已经记过的老坑：

- 内测期间 KAIYANG 在 `~/dev/Agent_talking/` 里干主要工作（脱 OneDrive 后的新 checkout）
- OneDrive 那个 checkout 是**镜像** — OneDrive sync 自己不会 `git pull`
- 我接到 task 时直接在 OneDrive checkout 干活，看到的 `git log` 是 OneDrive 上次 sync 时的 main，不是 origin/main
- `git status` 是干净的（OneDrive 文件层一致），但 `git log origin/main` 暴露落后

每次 force-push deploy 前**必须** `git fetch origin && git log origin/main..HEAD && git log HEAD..origin/main` 双向看一眼。我今天没看，差点上当。

修法两条：

1. **短期**：sync-deploy-branch.sh 顶部加一个 origin/main 偏差检测，本地 main != origin/main 时 die（要么 pull 要么显式 `--allow-divergent`）。
2. **长期**（昨天已经在做）：放弃 OneDrive checkout，全切 `~/dev/Agent_talking/`。今天这个事件再次说明就这条路。

---

## 数字总结

| 项 | 值 |
|---|---|
| 今日 commits | **1**（`e099fb2` web-app: route CONFLICT_REPORT to owning browser tabs） + 1 doc（本文） |
| 今日 sync-deploy | **2** 次（第一次回滚版，第二次正确版覆盖） |
| 今日 archive 分支 | 1（`deploy-archive-2026-04-26`，仍指向 4-25 晚的 b1f3b7c — 是真正可用的回滚点） |
| 今日发现 bug | **1**（CONFLICT_REPORT 路由不到 user:N 浏览器） |
| 今日新增代码 | 30 行（1 个 helper + 1 段 routing 扩展） |
| 今日反驳昨天假设 | 0 |
| 今日栽进老坑 | **1**（OneDrive checkout 落后 origin 13 commits — 跟 4-24/4-25 同源） |
| 今日 PyPI 发版 | 0 |
| 今日 Lightsail rebuild | 0（推到 origin/deploy 了，待 KAIYANG 自己上 VM 跑 `git fetch origin deploy && git reset --hard && docker compose up -d --build`） |

---

## Backlog 更新

**今天清掉**：

- ✅ Agent↔agent 场景下 web Conflicts 面板不渲染（`e099fb2`）

**进 backlog**：

- **前端：浏览器里能 Yield 自己 agent 的 intent**。当前 `myIntentInConflict` 判定漏 agent intent，导致 Conflicts 卡片只有 Acknowledge 没 Yield。修法是在 `useMpacSession` 加 owner-relationship tracking（把 `agent:user-N` 的 intent 也认成 "我的"）+ 把 `myIntentId` 解析改造支持 sibling
- **CONFLICT_REPORT 单元测试**。今天的 `agent_conflict_repro.py` 是 ad-hoc 写的，应该把 3-socket 路径放到 `web-app/api/mpac_smoke.py` 旁边作为 `mpac_agent_conflict_smoke.py` 之类。下次有人改 routing 能立刻发现回归
- **sync-deploy-branch.sh 加偏差检测**：本地 main 落后 origin/main 时 die（见事件二教训）
- **OneDrive checkout 全切走**（昨天已经在做，今天再次证明必要）

---

## Today's commits

1. `e099fb2` web-app: route CONFLICT_REPORT to owning browser tabs, not just agent principals
2. *(待提交)* Daily report 2026-04-26 — 本文

---

## 产品感：Routing 决策是否对称要看真实主体

修复本身 30 行很无聊，**有意思的是 bug 怎么这么久没被发现**。`mpac_smoke.py` 本来就测 user↔user 的 CONFLICT_REPORT，绿了 4 个月。但**真实产品流程**早就改成 agent 主导了（`page.tsx:1046-1063` 注释里写了"Intent announcement is owned by Claude's MCP, NOT the browser"），而 smoke test 没跟着改。结果协议层 contract 满足、UI 路径里**主体身份变了**，CONFLICT_REPORT 还按老主体路由 — 中间这段断层没人补。

**普遍的教训**：当**身份/主体抽象**演化（比如从 "user 直接发 intent" → "agent 代表 user 发 intent"），所有按主体做路由的代码（CONFLICT_REPORT、PROJECT_EVENT、未来的 NOTIFY 类消息）都要复审。今天暴露的就是 CONFLICT_REPORT 这一个，但 `_BROADCAST_INBOUND_TYPES` / `_UNICAST_RESPONSE_TYPES` 那两张表里每一项也都该问一遍："如果发起方是 agent，谁应该看到这条？"。今天没动那部分 — 进 backlog。

另一层教训在事件二：**部署脚本自己应该有"我和远端同步吗？"的自检**。`sync-deploy-branch.sh` 现在只检查 working tree 干净 + 在 main 分支 — 但没检查 main 是否落后 origin。落后时 force-push deploy = 静默回滚 prod。修法很简单（git rev-list origin/main..HEAD），今天**记下来了**。

---

## 生产健康（今天收工状态）

- `mpac` 仍 0.2.4（服务端核心今天没动）
- `mpac-mcp` 仍 0.2.5（昨天发的）
- origin/main = `e099fb2`（13 上游 + conflict-routing fix）
- origin/deploy = `f5ba410`（snapshot from e099fb2，含全量）
- 回滚点：`deploy-archive-2026-04-26` = 4-25 晚的 `b1f3b7c` 快照（如需回滚到我动手前的 prod state，`git reset --hard origin/deploy-archive-2026-04-26`）
- **Lightsail 还没 rebuild** — 容器仍跑昨天 4-25 晚 b1f3b7c 的 image。KAIYANG 自己 SSH 上去跑：

  ```bash
  ssh ubuntu@<static-ip>
  cd ~/Agent_talking
  git fetch origin deploy
  git reset --hard origin/deploy
  sudo docker compose -f deploy/aws-lightsail/docker-compose.yml \
      --env-file /etc/mpac/compose.env up -d --build
  ```

  然后开浏览器在两个 project tab 同时让 Claude 编同一文件，确认 Conflicts 面板有红卡片弹出。
- 本机 `web-app/api/mpac_bridge.py` 在 routing 段加了 6 行 + helper section 加了 21 行 = 30 行 total。0 行删除
- 本机 `.claude/launch.json` 本地多了一个 `web-app-conflict-test` entry（指 8002 + port 3010 dev server），gitignore 里所以不入仓 — 测试用 harness，留着以后还能用

---

**收工。**用户报的 bug 修了；OneDrive 这个老坑今天又抓了一次差点炸 prod，下次 deploy 前先 `git fetch origin && git log origin/main..HEAD` 必跑。
